import React, {Component} from 'react';

export default class Header extends Component {
    render(props) {
        return (
            <p> This is from the Header component </p>
            
        )
    }
}