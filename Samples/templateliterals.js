let a=10, b=20;

// Without using literals
console.log("Value of a = "+ a + " and value of b = " + b);

// Using literals
console.log(`Value of a = ${a} and value of b = ${b}`);

// Using a backtick ` for these strings also allows us to type these across multiple lines but being treated as a single string either way