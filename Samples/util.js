export const greet = () => console.log("Hello!");
// const let z = 6000;

let calc = {
    add: (a,b) => a+b,
    sub: (a,b) => a-b,
    mul: (a,b) => a*b,
    div: (a,b) => a/b
}

// export default z;